---
phase: "01"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260527000001_phase1_schema.sql
autonomous: true
requirements:
  - CALL-01
  - CALL-02
  - CALL-03
  - CALL-04
  - CALL-05
  - CALL-06
  - CALL-07
  - CALL-08
  - MBR-01
  - MBR-02
  - MBR-03
  - AUTH-02

must_haves:
  truths:
    - "org_units table exists with 8 seeded ward organizations in correct sort order"
    - "positions table exists with org_unit_id FK, is_active flag, and notes column"
    - "members table exists with name-only design (no contact info)"
    - "callings table exists with calling_status enum and state_entered_at column"
    - "Partial unique index callings_position_active_unique exists on callings table"
    - "calling_events append-only audit table exists with calling_id FK"
    - "session table exists for connect-pg-simple"
  artifacts:
    - path: "supabase/migrations/20260527000001_phase1_schema.sql"
      provides: "Full DDL: all 6 tables + enum + partial unique index + org_units seed data"
      contains: "CREATE TABLE callings"
  key_links:
    - from: "callings"
      to: "callings_position_active_unique partial index"
      via: "CREATE UNIQUE INDEX ... WHERE status NOT IN ('declined', 'released', 'cancelled')"
      pattern: "callings_position_active_unique"
    - from: "calling_events"
      to: "callings"
      via: "ON DELETE CASCADE FK"
      pattern: "REFERENCES callings"
---

<objective>
Write the complete Phase 1 database migration SQL file. This includes all table DDL, the calling_status enum, partial unique index, org_units seed data, and session table for connect-pg-simple. No application code — only the SQL migration artifact.

Purpose: The schema is the source of truth for the entire calling pipeline. All backend routes and frontend types derive from it. Wave 2 applies this migration to Supabase.
Output: `supabase/migrations/20260527000001_phase1_schema.sql` with complete Phase 1 DDL.
</objective>

<execution_context>
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-RESEARCH.md (Database Schema section — use verbatim DDL)
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-CONTEXT.md (D-10 through D-13)

<interfaces>
<!-- Full DDL from RESEARCH.md — implement verbatim, no deviations -->

Locked decisions affecting schema:
- D-10: Vacancy = JOIN result. Do NOT add 'vacant' to calling_status enum.
- D-11: calling_events is append-only audit log — no UPDATE/DELETE routes will target it.
- D-12: Partial unique index UNIQUE(position_id) WHERE status NOT IN ('declined', 'released', 'cancelled')
- D-13: state_entered_at column on callings — NOT NULL, defaults to now(), must be set to now() on every transition.

ANTI-PATTERN WARNING: Do NOT add interview_event_id or set_apart_event_id columns to callings. Those belong to Phase 2. Keep Phase 1 schema clean.

calling_status enum values (complete, in order):
  'recommended', 'extended', 'accepted', 'declined', 'sustained', 'set_apart', 'released', 'cancelled'

org_units seed (standard LDS ward structure, per Claude's Discretion):
  ('Bishopric', 1), ('Elders Quorum', 2), ('Relief Society', 3),
  ('Young Men', 4), ('Young Women', 5), ('Primary', 6),
  ('Sunday School', 7), ('Ward', 8)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 1 database migration SQL file</name>
  <read_first>
    /Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-RESEARCH.md (entire Database Schema section)
    /Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-CONTEXT.md (D-10, D-11, D-12, D-13)
  </read_first>
  <files>
    supabase/migrations/20260527000001_phase1_schema.sql
  </files>
  <action>
    1. Create directory `supabase/migrations/` at the project root.

    2. Create file `supabase/migrations/20260527000001_phase1_schema.sql` with the exact DDL from RESEARCH.md Database Schema section. The file must contain in order:

    a. `org_units` table (id UUID PK, name TEXT NOT NULL, sort_order INT NOT NULL DEFAULT 0, created_at TIMESTAMPTZ DEFAULT now())

    b. `positions` table (id UUID PK, org_unit_id UUID REFERENCES org_units ON DELETE CASCADE, name TEXT NOT NULL, sort_order INT NOT NULL DEFAULT 0, is_active BOOLEAN NOT NULL DEFAULT true, notes TEXT, created_at TIMESTAMPTZ DEFAULT now(), UNIQUE(org_unit_id, name))

    c. `members` table (id UUID PK, name TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT now())

    d. `calling_status` ENUM type with all 8 values: 'recommended', 'extended', 'accepted', 'declined', 'sustained', 'set_apart', 'released', 'cancelled'

    e. `callings` table (id UUID PK, position_id UUID NOT NULL REFERENCES positions ON DELETE CASCADE, member_id UUID REFERENCES members ON DELETE SET NULL, status calling_status NOT NULL DEFAULT 'recommended', bishopric_owner TEXT, notes TEXT, state_entered_at TIMESTAMPTZ NOT NULL DEFAULT now(), created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now())

    f. Partial unique index:
    ```sql
    CREATE UNIQUE INDEX callings_position_active_unique
      ON callings (position_id)
      WHERE status NOT IN ('declined', 'released', 'cancelled');
    ```

    g. `calling_events` table (id UUID PK, calling_id UUID NOT NULL REFERENCES callings ON DELETE CASCADE, from_status calling_status, to_status calling_status NOT NULL, note TEXT, created_at TIMESTAMPTZ DEFAULT now())

    h. `session` table for connect-pg-simple:
    ```sql
    CREATE TABLE IF NOT EXISTS session (
      sid    VARCHAR NOT NULL COLLATE "default" PRIMARY KEY,
      sess   JSON    NOT NULL,
      expire TIMESTAMP(6) NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_session_expire ON session(expire);
    ```

    i. Seed org_units with 8 standard LDS ward organizations in sort_order 1-8 (Bishopric=1 through Ward=8).

    Add a header comment block at the top of the file:
    ```sql
    -- Phase 1: Calling Pipeline & Auth — Database Schema
    -- Apply via: supabase db push
    -- Locks implemented: D-10 (vacancy=JOIN), D-11 (events log), D-12 (partial unique index), D-13 (state_entered_at)
    -- IMPORTANT: Do NOT add interview_event_id or set_apart_event_id — those belong to Phase 2
    ```
  </action>
  <verify>
    <automated>grep -c "callings_position_active_unique" /Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/supabase/migrations/20260527000001_phase1_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/migrations/20260527000001_phase1_schema.sql`
    - File contains `CREATE TYPE calling_status AS ENUM` with all 8 status values
    - File contains `CREATE UNIQUE INDEX callings_position_active_unique`
    - File contains `WHERE status NOT IN ('declined', 'released', 'cancelled')` (the partial index WHERE clause)
    - File contains `state_entered_at TIMESTAMPTZ NOT NULL` in callings DDL
    - File contains `CREATE TABLE calling_events` with `REFERENCES callings(id) ON DELETE CASCADE`
    - File contains `CREATE TABLE IF NOT EXISTS session` for connect-pg-simple
    - File contains 8 INSERT rows into org_units (Bishopric through Ward)
    - File does NOT contain `interview_event_id` or `set_apart_event_id` (Phase 2 only)
    - File does NOT contain `'vacant'` in the calling_status enum definition
    - Verify: `grep -c "state_entered_at TIMESTAMPTZ NOT NULL" supabase/migrations/20260527000001_phase1_schema.sql` returns 1
    - Verify: `grep -c "Bishopric" supabase/migrations/20260527000001_phase1_schema.sql` returns 1
  </acceptance_criteria>
  <done>Migration SQL file written with complete Phase 1 schema including all 6 tables, enum, partial unique index, session table, and org_units seed data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase | DDL applied with service_role; public schema, no RLS needed (internal tool) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | callings table | mitigate | Partial unique index at DB level prevents duplicate active pipeline entries regardless of application logic bugs |
| T-02-02 | Tampering | calling_events | mitigate | ON DELETE CASCADE ensures no orphaned audit events; no UPDATE route planned — append-only enforced at application layer |
| T-02-03 | Info Disclosure | session table | accept | Session data is `{ authenticated: true }` only — no member names or password hash stored in session |
</threat_model>

<verification>
After task completes:
- `grep -c "callings_position_active_unique" supabase/migrations/20260527000001_phase1_schema.sql` returns 1
- `grep -c "state_entered_at TIMESTAMPTZ NOT NULL" supabase/migrations/20260527000001_phase1_schema.sql` returns 1
- `grep -c "calling_events" supabase/migrations/20260527000001_phase1_schema.sql` returns >= 2 (table creation + FK reference)
- File does not contain the string "vacant" inside an ENUM or CREATE TYPE statement
</verification>

<success_criteria>
Migration SQL file is complete, contains all Phase 1 tables with correct constraints, partial unique index, and org_units seed data. Ready for `supabase db push` in Wave 2.
</success_criteria>

<output>
After completion, create `/Users/luxveritus/Documents/AI PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-02-SUMMARY.md`
</output>
