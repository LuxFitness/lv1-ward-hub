---
phase: "01"
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - supabase/config.toml
autonomous: true
requirements:
  - CALL-07
  - AUTH-02

must_haves:
  truths:
    - "All 6 Phase 1 tables exist in the Supabase project (org_units, positions, members, callings, calling_events, session)"
    - "calling_status enum exists in the public schema"
    - "Partial unique index callings_position_active_unique exists on callings table"
    - "org_units table contains 8 rows (Bishopric through Ward)"
    - "session table exists for connect-pg-simple"
  artifacts:
    - path: "supabase/migrations/20260527000001_phase1_schema.sql"
      provides: "Applied migration — tables now exist in Supabase"
  key_links:
    - from: "supabase db push"
      to: "Supabase Postgres"
      via: "migration file applied to remote project"
      pattern: "callings_position_active_unique"

user_setup:
  - service: supabase
    why: "Supabase CLI needs access token to push to the remote project"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens → Generate new token"
    dashboard_config:
      - task: "Link project to local Supabase CLI"
        location: "Run: supabase link --project-ref YOUR_PROJECT_REF (find ref in Dashboard → Settings → General)"
---

<objective>
[BLOCKING] Apply the Phase 1 schema migration to the Supabase project and verify all tables, the enum, partial unique index, and seed data exist. This plan is a required gate — no backend API route can be built until the schema is applied.

Purpose: Without the schema applied, all subsequent calling pipeline and member CRUD routes will fail at query time. This is the blocking prerequisite for Wave 3+ work.
Output: Live Supabase project with Phase 1 schema applied and verified.
</objective>

<execution_context>
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-RESEARCH.md (Database Schema section, Common Pitfall 2: Partial Unique Index Missed)
@/Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-CONTEXT.md (D-12)
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push schema migration and verify Supabase tables</name>
  <read_first>
    supabase/migrations/20260527000001_phase1_schema.sql
    /Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-RESEARCH.md (Common Pitfall 2 — Partial Unique Index Missed During Migration)
  </read_first>
  <files>
    supabase/config.toml
  </files>
  <action>
    1. Initialize Supabase project config if not already present:
    ```bash
    cd /Users/luxveritus/Documents/AI\ PROJECTS/lv1-ward-hub
    supabase init
    ```
    This creates `supabase/config.toml`. If config.toml already exists, skip this step.

    2. Link to the remote Supabase project (requires SUPABASE_ACCESS_TOKEN in environment or Supabase CLI logged in):
    ```bash
    supabase link --project-ref YOUR_PROJECT_REF
    ```
    Find the project ref in Supabase Dashboard → Settings → General → Reference ID.

    3. Push the migration:
    ```bash
    SUPABASE_ACCESS_TOKEN=your-token supabase db push
    ```
    If already logged in via `supabase login`, the token env var is not needed.

    4. Verify the partial unique index exists (this is the critical check — see Pitfall 2):
    ```bash
    supabase db query "SELECT indexname FROM pg_indexes WHERE tablename = 'callings';"
    ```
    Expected output must include `callings_position_active_unique`.

    5. Verify org_units seed data:
    ```bash
    supabase db query "SELECT name, sort_order FROM org_units ORDER BY sort_order;"
    ```
    Expected: 8 rows from Bishopric (1) to Ward (8).

    6. Verify calling_status enum:
    ```bash
    supabase db query "SELECT unnest(enum_range(NULL::calling_status));"
    ```
    Expected: 8 rows (recommended, extended, accepted, declined, sustained, set_apart, released, cancelled).

    7. Verify session table:
    ```bash
    supabase db query "SELECT table_name FROM information_schema.tables WHERE table_name = 'session';"
    ```
    Expected: 1 row with `session`.

    If any verification fails, do NOT proceed. Review the migration SQL file for the missing DDL and correct it before re-pushing.
  </action>
  <verify>
    <automated>supabase db query "SELECT indexname FROM pg_indexes WHERE tablename = 'callings' AND indexname = 'callings_position_active_unique';" 2>/dev/null | grep -c "callings_position_active_unique"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 with no errors
    - `SELECT indexname FROM pg_indexes WHERE tablename = 'callings';` output includes `callings_position_active_unique`
    - `SELECT COUNT(*) FROM org_units;` returns 8
    - `SELECT unnest(enum_range(NULL::calling_status));` returns 8 rows
    - `SELECT table_name FROM information_schema.tables WHERE table_name = 'session';` returns 1 row
    - `SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name IN ('org_units','positions','members','callings','calling_events');` returns 5 rows
  </acceptance_criteria>
  <done>All Phase 1 tables exist in Supabase, partial unique index verified, 8 org_units seeded, session table ready for connect-pg-simple.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Applied Phase 1 database schema to Supabase. All tables, enum, partial unique index, and seed data are live in the Supabase project.</what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → Table Editor
    2. Confirm these tables are visible: org_units, positions, members, callings, calling_events, session
    3. Click on org_units → confirm 8 rows (Bishopric through Ward with sort_order 1-8)
    4. Open Database → Indexes → confirm `callings_position_active_unique` is listed for the callings table
    5. Open Database → Enumerations (or run in SQL editor): `SELECT unnest(enum_range(NULL::calling_status));` — confirm 8 enum values
  </how-to-verify>
  <resume-signal>Type "schema verified" if all tables and index are confirmed, or describe what's missing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase CLI → Remote Supabase project | Migration applied with service account credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Partial unique index | mitigate | Explicit verification query confirms index exists before proceeding to API implementation |
| T-03-02 | Denial of Service | Migration failure | accept | If push fails, subsequent plans cannot proceed — explicit checkpoint blocks progress until resolved |
</threat_model>

<verification>
Schema push verified when:
- All 5 application tables exist (org_units, positions, members, callings, calling_events)
- session table exists
- callings_position_active_unique index confirmed via pg_indexes query
- 8 org_units rows confirmed
- 8 calling_status enum values confirmed
</verification>

<success_criteria>
Supabase project has the complete Phase 1 schema applied and verified. All downstream backend route plans can now reference real table structures.
</success_criteria>

<output>
After completion, create `/Users/luxveritus/Documents/AI PROJECTS/lv1-ward-hub/.planning/phases/01-calling-pipeline-and-auth/01-03-SUMMARY.md`
</output>
